# تحليل كود SodiumEditor واقتراح تحسينات للأداء

هذا التقرير يتناول تحليل ملفات المشروع المرفق (SodiumEditor) والبحث عن نقاط ضعف محتملة في الأداء أو مشاكل في تصميم النظام خاصة مع الملفات الضخمة (حتى 10 جيجا بايت). تم استعراض معظم ملفات الكود والبحث عن أماكن قد تكون مؤثرة على السرعة أو الذاكرة أو تجربة المستخدم، إضافة إلى اقتراح تعديلات ممكنة.

## ملاحظات عامة على الكود

* **وجود فروع كود غير قابلة للتنفيذ** – توجد عدة أماكن في الكود تحتوى على عبارات شرطية مثل `false ? … : …` أو `if (false) { … }`، وهو ما يجعل جزءًا من الكود غير مستخدم إطلاقًا. مثال على ذلك في كلاس `FileIO`، حيث يتم تحديد حجم ذاكرة التخزين المؤقت لأسطر الملف باستخدام شرط ثابت `false ? 500 : 250`【478361451738046†L61-L64】؛ الأمر نفسه يتكرر في حساب عدد الأسطر المعروضة في `ScrollBounds`【478423104493321†L34-L36】، وفي حساب إجمالى الأسطر المرئية بـ `WordWrap`【645744848755401†L378-L386】، وأيضاً في كلاس `Cursor`【513164122247785†L468-L470】. هذه الشروط غير منطقية وقد تكون بقايا تجارب أو تصحيح، وينبغى إزالتها أو استبدالها بمتغيرات إعداد حقيقية لتحسين وضوح الكود وإمكانية تعديله.

* **حجم ذاكرة التخزين المؤقت للسطر المباشر (directLineCache) صغير وغير ديناميكي** – الكود يحدد حجم التخزين المؤقت للأسطر المقروءة مباشرة بقيمة ثابتة (250 سطر)، ولا يتم تعديلها بناء على حجم الشاشة أو قدرة الجهاز. كما أن شرط `false ? 500 : 250` يجعل اختيار حجم أكبر غير مستخدم【478361451738046†L61-L64】. عند التعامل مع ملفات ضخمة ومع أجهزة ذات ذاكرة أكبر يمكن زيادة هذا الحجم أو جعله قابلاً للتعديل.

* **المسح الخطى للملف لاستخدامه في عمليات تحديد المدى** – الكلاس `ByteRangeLocator` يعتمد على قراءة الملف بايتاً‑ببايت لتحديد مواقع بداية الأسطر، عبر دالة `findTwoLineStartBytesByScanning` التي تستخدم `RandomAccessFile.read()` فى حلقة حتى يتم الوصول للسطر المطلوب【931486195522859†L65-L80】. هذه الطريقة بطيئة للغاية مع الملفات الكبيرة، خاصة مع الترميز المتغير مثل UTF‑8. كما أن `computeByteRangeUsingIndex` يقرأ الملف مرتين للوصول إلى السطرين ثم حساب المسافة بينها【931486195522859†L20-L34】.

* **إنشاء فهرس الأسطر (FileIndexer) يمر على الملف مرتين** – دالة `buildIndexJava` تقرأ الملف فى المرة الأولى لحساب عدد الأسطر (من أجل تخصيص مصفوفة الحجم الصحيح) ثم تعاود قراءته لتخزين إزاحات الأسطر. هذا الأسلوب يضاعف زمن القراءة ولا يستغل المعالجات متعددة النوى.

* **استخدام `RandomAccessFile` فى كل عملية قراءة** – كل مرة يتم فيها تحميل نافذة جديدة من الملف أو قراءة مقطع صغير يتم فتح كائن `RandomAccessFile` جديد ثم إغلاقه. عمليات الفتح والإغلاق هذه مكلفة. كما أن القراءة تتم بوساطة مصفوفات صغيرة نسبياً (8192 بايت)، مما يؤدى إلى عدد كبير من عمليات الـ I/O مع الملفات الكبيرة.

* **حجم النافذة والإستباق** – المتغيرات `windowSize` و `prefetchLines` فى `WindowRender` محددة إلى 250 سطر لكل منهما. عند تشغيل محرر على هاتف بشاشة صغيرة قد يكون هذا العدد كبيراً، بينما مع شاشات أكبر قد يكون قليلاً. عدم وجود خوارزمية لتعديل حجم النافذة حسب سرعة التمرير وحجم الشاشة قد يؤثر على الأداء وذاكرة الجهاز.

* **وجود حد أعلى لعدد المطابقات فى البحث** – كلاس `Search` يحد النتائج عند 1000 مطابقة لتجنب مشاكل الأداء. هذا قد يربك المستخدم عند البحث فى ملفات كبيرة لأنه لن يعثر على كل النتائج.

* **تحديد عرض النص وحساب التفاف الأسطر (WordWrap) مكلف** – الكود يحسب متوسط عرض الحرف لكل سطر ثم يعيد حساب مقاييس الالتفاف عند كل تغيير. تلك العمليات تتم على مؤشر ترابط واجهة المستخدم، وقد تسبب بطئاً عند فتح ملفات كبيرة أو استخدام خطوط متغيرة العرض.

## اقتراحات لتحسين الأداء

### 1. تحسين عمليات القراءة I/O

1. **استخدام `FileChannel` وذاكرة‑مؤشرة (`MappedByteBuffer`)**: بدلاً من فتح `RandomAccessFile` لكل عملية وقراءة المصفوفات الصغيرة، يمكن فتح `FileChannel` مرة واحدة لكل ملف، واستخدام `MappedByteBuffer` لتمثيل أجزاء من الملف فى الذاكرة. يمكن تحديد حجم الخريطة حسب النافذة المطلوبة (مثلاً 1 ميغابايت حول السطر المراد). هذا يقلل من عدد عمليات القراءة النظامية ويستفيد من آليات caching فى نظام التشغيل.

2. **جمع تمريرات بناء الفهرس فى تمريرة واحدة**: يمكن استخدام بنية بيانات ديناميكية (مثل `LongArrayList`) أثناء المرور الأول لتخزين إزاحات الأسطر، دون الحاجة لحساب عدد الأسطر مسبقاً. بعد انتهاء القراءة يتم تحويل القائمة إلى مصفوفة. بهذه الطريقة يُقرأ الملف مرة واحدة فقط بدلاً من مرتين، ويُستفاد من التخزين المؤقت لنظام التشغيل.

3. **زيادة حجم المخابئ المؤقتة وتكييفها**: تغيير حجم المصفوفة المستخدمة فى القراءة من 8 ك.ب إلى 64 ك.ب أو 128 ك.ب سيقلل عدد عمليات النظام. يمكن جعل الحجم قابلاً للتعديل فى الإعدادات بناءً على حجم الذاكرة المتوفرة فى الجهاز.

4. **إعادة استخدام كائنات القراءة**: بدلاً من إنشاء `RandomAccessFile` جديد فى كل مرة، يمكن إنشاء واحد أو اثنين لكل خيط عمل والاحتفاظ بهما فى كائنات محلية (أو استخدام تجمع موارد). هذا يقلل من تكاليف فتح الملفات المتكرر.

5. **استخدام الترميز المعيارى عند القراءة**: عند تحويل بايتات إلى نص يمكن استخدام `CharsetDecoder` مع buffer قابل لإعادة الاستخدام، مما يقلل من إنشاء كائنات `String` مؤقتة. كما أن التحويل الجزئى فى السطور الطويلة يمكن أن يستفيد من آليات `CharBuffer` بدلاً من إنشاء نصوص جديدة لكل مقطع.

### 2. تحسين إنشاء الفهرس وإدارته

1. **فهرسة تدريجية (Incremental Indexing)**: بدلاً من بناء الفهرس الكامل لجميع الأسطر عند فتح الملف، يمكن إنشاء الفهرس بشكل تدريجى مع التنقل عبر الملف. فمثلاً عند تحميل نافذة معينة يتم فهرسة تلك المنطقة وبعض المناطق حولها فى الخلفية. هذا يخفف من فترة الانتظار الأولى ويقلل استهلاك الذاكرة عند الملفات ذات عدد أسطر هائل. عند الحاجة إلى الذهاب إلى آخر الملف، يمكن طلب فهرسة الجزء المتبقى فى الخلفية.

2. **ضغط الفهرس أو استخدام بنية شجرية**: إذا كان الملف يحتوى على مئات الملايين من الأسطر فإن استخدام مصفوفة من نوع `long` لكل سطر سيستهلك عشرات الميغا من الذاكرة. يمكن تقليل ذلك باستخدام ترميز الفرق (delta encoding) أو الاعتماد على بنية شجرية (مثل B‑tree) تخزن كل مسافة بين الأسطر بدلاً من إزاحة مطلقة. كما يمكن استخدام `int` بدلاً من `long` إذا كان حجم الملف أقل من 2 جيجا بايت.

3. **تعديل شرط تعطيل الفهرسة**: حالياً يتم تعطيل الفهرسة عندما تتجاوز الذاكرة المخصصة حدوداً ثابتة، لكن هذا قد لا يكون مناسباً لجميع الأجهزة. يُفضَّل إضافة إعداد يسمح للمستخدم بالتحكم فى تشغيل الفهرسة أو إيقافها، مع عرض رسالة تحذير إذا كان الملف كبيراً جدًا.

### 3. تحسين التخزين المؤقت للنص

1. **تكبير ذاكرة التخزين المؤقت للدوال المباشرة `directLineCache`**: يمكن جعل قيمة `maxCacheSize` تعتمد على حجم الشاشة وذاكرة الجهاز، أو توفير خيار للمستخدم لزيادتها. كما يجب إزالة الكود غير المستخدم الذى يحدد الشرط الثابت `false ? 500 : 250`【478361451738046†L61-L64】 واستبداله بشرط منطقى مثل تفضيل الأجهزة ذات ذاكرة كبيرة.

2. **استخدام استراتيجية LRU حقيقية**: يمكن استخدام مكتبات جاهزة لتنفيذ caching مع وسياسة LRU تناسب الحمل، مثل `LinkedHashMap` مع حدود ديناميكية أو `LruCache` المتوفرة فى Android.

3. **تقليل حجم النافذة الافتراضية**: بدلاً من تحميل 250 سطر لكل نافذة و250 سطر إضافى للاستباق، يمكن حساب حجم النافذة بناءً على ارتفاع الشاشة وعدد الأسطر المرئية. كما يمكن تقليل عدد الأسطر التى يتم جلبها عند التمرير السريع (fast scrolling) للحفاظ على الذاكرة والسرعة.

4. **استبدال مصفوفة الأسطر فى `WindowRender` ببنية حلقيّة**: عند تحريك النافذة للأمام أو للخلف يتم تفريغ وإعادة ملء `ArrayList` بالكامل. يمكن استخدام بنية حلقيّة (circular buffer) بحيث يتم إعادة استخدام نفس الحاوية وتحديث المؤشر إلى بداية النافذة، مما يقلل من تخصيص الذاكرة وعمليات النقل.

### 4. تحسين خوارزميات البحث والتعامل مع النص

1. **استخدام خوارزميات بحث فعّالة**: فى الكود الحالى يتم المرور على السطور بشكل تسلسلى للبحث عن نص، مع حد أقصى للمطابقات يصل إلى 1000 نتيجة. يمكن استخدام خوارزميات مثل Boyer–Moore أو Knuth–Morris–Pratt للبحث داخل كل سطر، أو استخدام أدوات مثل `Pattern` مع تفعيل خيارات الـ `Matcher` واستخدام `find` المتكرر. كما يمكن رفع أو إزالة حد النتائج وتقديم خيار للمستخدم لتحديد عدد النتائج المعروضة.

2. **الكشف عن الكلمات المفتاحية أثناء القراءة**: يمكن دمج عملية البحث مع عملية تحميل النافذة بحيث يتم تسجيل الأماكن التى تحتوى على النص المطلوب عند قراءة السطر من القرص، مما يقلل من عدد القراءات اللاحقة.

3. **تحسين قراءة المقاطع `ByteRangeLocator` باستخدام الفهرس**: إذا كان الفهرس جاهزاً، يمكن استخدامه لحساب إزاحة البداية والنهاية مباشرة، بدلاً من القراءة المتتابعة بايتاً‑ببايت كما فى `findLineStartByteByScanning`【931486195522859†L65-L80】. كما يمكن استخدام مصفوفة كبيرة لتحميل عدة أسطر دفعة واحدة ثم البحث عن موقع السطر المطلوب.

### 5. تحسين العرض والرسم (Rendering)

1. **إزالة الأكواد غير المستخدمة**: التعليمات الشرطية الثابتة مثل `if (false) {…}` فى `TextRender`، `ViewRender` وغيرها تُربك القارئ وتمنع المحول البرمجى من تحسين الكود بشكل صحيح. يجب حذف هذه الأقسام أو استبدالها بمعامل ضبط حقيقى أو تعليقات توضح سبب إيقافها【513164122247785†L468-L470】【478423104493321†L34-L36】.

2. **الاستفادة من GPU**: يمكن تفعيل طبقة الرسم العتادية (`setLayerType(View.LAYER_TYPE_HARDWARE)`) للرسم على الجهاز، مما يسمح لنظام Android باستخدام المسرّع الرسومى لرسم النص والأشكال. كما أن استخدام مكونات مثل `RecyclerView` مع عناصر نصية قابلة لإعادة الاستخدام قد يحسن الأداء عند التمرير السريع، لأن Android سيقوم بتدوير عناصر العرض بدلاً من إعادة رسم كل محتوى النافذة.

3. **تقليل عمليات قياس النص**: يقوم المحرر بقياس عرض النص لكل سطر عبر `Paint.measureText()` لحساب متوسط عرض الحرف والتفاف السطر. يمكن تخزين عرض بعض الأحرف الثابتة (مثل الحرف "m") واستخدامه كتقدير بدلاً من حساب متوسط كامل للسطر، خصوصاً مع الخطوط أحادية العرض. عند الخطوط المتغيرة يمكن حساب المتوسط مرة واحدة لكل خط وتخزينه فى Cache أكبر، مع تجديده فقط عند تغيير الخط أو الحجم.

4. **تحميل جزئى للأسطر الطويلة**: الميكانيكية الحالية تقوم بتحميل جزء من السطر الطويل عند ظهوره لأول مرة (`shouldStreamLineLength` ثم `readLineSlice…`) ثم تطلب المزيد عند الحاجة. يمكن تطوير آلية لتمثيل السطر الطويل فى كائن `CharSequence` مرتبط بالملف، بحيث يتم قراءة الرموز المطلوبة فقط عند قياس أو رسم النص. هذا يحمى الذاكرة من إنشاء سلاسل طويلة لأسطر قد لا يتم عرضها كاملة.

5. **تحسين مؤشرات اللفة (WordWrap) والزمن المستغرق**: عملية بناء مقاييس الالتفاف (`wrapMetrics`) تتم أحيانًا كاملة أثناء الفتح، وقد تؤخر عرض الملف. يمكن حساب مقاييس الالتفاف للنافذة المرئية فقط، وتأجيل باقى المعالجة إلى حين الحاجة. كما يمكن تشغيل العمليات الثقيلة على خيوط منفصلة حتى لا تؤثر على واجهة المستخدم.

### 6. تحسين إدارة المؤشرات والخيوط (Threading)

1. **تقسيم الخيوط حسب المهام**: فى الوقت الحالى يتم استخدام خيط واحد (`HandlerThread`) للتعامل مع القراءة، البناء، الفهرسة، وحساب المرشدات (bracket guides). يمكن إنشاء مسبح مهام (`ExecutorService`) أو استخدام `WorkManager` لتوزيع هذه العمليات على عدة خيوط مع إعطاء أولويات مختلفة. فمثلاً، يمكن معالجة الطلبات الخاصة بالنافذة الحالية فى خيط سريع، بينما تُعالج عمليات الفهرسة أو البحث فى الخلفية بخيط آخر.

2. **استخدام Future/Callback بدلاً من مؤشرات الإصدار**: فى كثير من الأماكن يتم التحقق من رقم نسخة المهام (`ioTaskVersion`) لمقارنة ما إذا كان العمل لا يزال صالحاً. يمكن استخدام هياكل مستقبلية (مثل `CompletableFuture`) أو مكتبات تزامن أعلى مستوى لإدارة إلغاء المهام بشكل أوضح وتقليل تعقيد الكود.

### 7. تحسين الميزات الثقيلة وتكيفها مع حجم الملف

1. **تعطيل الأدلة (indent guides, bracket guides) والتظليل التلقائى عند الملفات الكبيرة**: على الرغم من وجود متغير `heavyFeaturesThreshold` الذى يعطل بعض الميزات عند الملفات الكبيرة، إلا أنه يمكن إضافة المزيد من المنطق لتعطيل الميزات تدريجياً حسب عدد الأسطر أو حجم الملف بدلاً من الإيقاف المفاجئ. كما يمكن السماح للمستخدم باختيار الميزات التى يرغب بتفعيلها من خلال الإعدادات.

2. **تحسين خوارزمية التظليل (Highlighting)**: إذا كان المبرز اللغوى (Syntax Highlight) يستخدم تحليلًا مكلفًا لكل السطور، يمكن قصر التظليل على النافذة المرئية فقط أو استخدام مكتبة أسرع تدعم incremental parsing.

### 8. اقتراحات تنظيمية أخرى

* **تنظيف الكود وترتيبه**: وجود أقسام غير مستخدمة أو شروط ثابتة يجعل الكود صعب القراءة والصيانة. يفضل إزالتها أو تبديلها بمعاملات إعداد قابلة للضبط.

* **إضافة اختبارات قياس الأداء**: لتأكيد فعالية التحسينات، يجب إضافة اختبارات تقيس سرعة فتح الملفات، سرعة التمرير، واستهلاك الذاكرة قبل وبعد التحسينات. يمكن استخدام أدوات Android Profiler وSystrace لتحديد النقاط الساخنة فى الكود.

* **توفير إعدادات للمستخدم**: يسمح للمستخدم باختيار حجم النوافذ والذاكرة المخصصة للتخزين المؤقت، وتمكين/تعطيل الميزات الثقيلة، واختيار وضع المعالجة عند التعامل مع ملفات ضخمة.

## خلاصة

تمكن SodiumEditor من فتح ملفات ضخمة بفضل اعتماده على القراءة الجزئية وعدم تحميل الملف بالكامل فى الذاكرة. ومع ذلك، تظهر عدة نقاط يمكن تحسينها لتعزيز الأداء خصوصاً على الأجهزة ذات الذاكرة المحدودة أو مع الملفات الضخمة:

* التخلص من الأكواد غير المستخدمة والظروف الثابتة، وتصحيح المتغيرات غير المنطقية مثل حجم التخزين المؤقت للأسطر 【478361451738046†L61-L64】.
* تحسين عمليات القراءة بإستخدام `FileChannel` أو `MappedByteBuffer`، وتمرير واحد لبناء الفهرس.
* جعل التخزين المؤقت والنافذة ديناميكية وتكييفها مع الجهاز وحجم الملف.
* تحسين خوارزميات البحث والتظليل، وتعطيل الميزات الثقيلة عند الحاجة.
* استخدام خيوط متعددة ومسبح مهام لتوزيع العمل الثقيل ومنع تجميد واجهة المستخدم.

اتباع هذه الاقتراحات سيساعد على تحسين السرعة، تقليل زمن استجابة الواجهة، وتخفيض استهلاك الذاكرة عند التعامل مع ملفات كبيرة.
